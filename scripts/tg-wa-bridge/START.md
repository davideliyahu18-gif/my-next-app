# חיבור וואטסאפ — קוד לטלפון

מופע: `710722683401`  
ערוץ: [`@newsil5`](https://t.me/newsil5) → קבוצה: `חמ״ל התרעות ירי איראן 🛡️` (מודגש, בלי הצגת מודיעין גלוי)

## אם הקוד לא עובד (חשוב)

אחרי קוד שגוי המופע נתקע. לפני ניסיון חדש:

1. בתוך המופע לחץ **אתחול / Reboot** (או Restart instance)
2. חכה ~30 שניות עד שהסטטוס חוזר ל-**לא מורשה**
3. רק אז בקש קוד חדש

### סדר נכון של הקוד

| איפה מופיע הקוד | איפה מזינים אותו |
|------------------|-------------------|
| במסך Green API | **בוואטסאפ בטלפון** |

1. Green API → המופע → **קישור לפי מספר טלפון**
2. הזן מספר בפורמט `9725xxxxxxxx` (בלי `+`, בלי `0` בהתחלה)
3. יופיע קוד (למשל `AB12CD34`)
4. בטלפון → WhatsApp → **מכשירים מקושרים** → **קישור מכשיר**
5. לחץ **קישור באמצעות מספר טלפון** (לא סריקה)
6. הזן **בדיוק** את הקוד מ-Green API, מהר

### גם בדוק

- יש עד **4 מכשירים מקושרים** — מחק ישנים (WhatsApp Web / מחשב)
- השתמש בוואטסאפ **הרשמי** (לא עותק / GB)
- אל תבקש קוד מכמה מקומות במקביל
- אם הסטטוס `starting` — שוב **אתחול** למופע

## סטטוס חיבור (עודכן)

- מופע: `710722683401` — מורשה
- **מספר מקושר בברקוד/QR (הבוט):** `054-967-6816` (`972549676816`)
- **מספר אישי לקבלת צלצול (לא נסרק):** `052-312-3944` — מקבל העתק פרטי של כל הודעה + תקינות
- קבוצה: `חמ״ל התרעות ירי איראן 🛡️` → `120363410746391414@g.us`
- ערוץ: `@newsil5`

> חשוב: המספר שנסרק ל־Green API **לא** מקבל צלצול על הודעות שהוא עצמו שולח.
> לכן הצלצול ל־3944 מגיע כהודעה פרטית מהבוט (`TG_WA_RING_CHAT_IDS`).

### יציבות (מומלץ)

הפעלה עם restart אוטומטי:

```bash
npm run tg-wa:supervise
```

זה מריץ את הפולר ומפעיל מחדש אחרי קריסה (backoff).  
מצב חי נשמר ב-`scripts/tg-wa-bridge/.data/` (seen + heartbeat).

פקודות בקבוצה: `עזרה` | `סטטוס` | `מקור` | `בדיקה` | `אחרון`

ב-Vercel (גיבוי / cron):

```bash
GREEN_API_INSTANCE=710722683401
GREEN_API_TOKEN=...apiTokenInstance...
TG_WA_WHATSAPP_CHAT_ID=120363409236894886@g.us
CRON_SECRET=...
```

אחרי Deploy — ping כל 1–2 דקות ל-`/api/cron/tg-wa-bridge`.
